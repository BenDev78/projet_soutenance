<nav class="pt-30 pb-30 bg-light text-center navigation_1">
    <div class="container-fluid px-5">
        <div class="row justify-content-center align-items-center f-18">
            <div class="mb-20 mb-lg-0 col-lg-3">
                <a href="{{ path('default_index') }}" class="link color-main f-32 bold megrim">
                    <i class="fal fa-wine-glass-alt action-2"></i> Cognac Guy Bonnaud
                </a>
            </div>
            <div class="col-lg-6">
                <a href="{{ path('admin_dashboard') }}" class="link mx-15{% if current_page == "index_admin" %} action-2{% else%} color-main{% endif %}">
                    Home
                </a>
                <a href="{{ path('admin_users') }}" class="link mx-15{% if current_page == "users_admin" %} action-2{% else%} color-main{% endif %}">
                    Utilisateurs
                </a>
                <a href="{{ path('admin_commands') }}" class="link mx-15{% if current_page == "commands_admin" %} action-2{% else%} color-main{% endif %}">
                    Commandes
                </a>
                <a href="{{ path('admin_products') }}" class="link mx-15{% if current_page == "products_admin" %} action-2{% else%} color-main{% endif %}">
                    Produits
                </a>
                <a href="{{ path('admin_carriers') }}" class="link mx-15{% if current_page == "carrier_admin" %} action-2{% else%} color-main{% endif %}">
                    Transporteurs
                </a>
                <a href="{{ path('admin_actualities') }}" class="link mx-15{% if current_page == "actualities_admin" %} action-2{% else%} color-main{% endif %}">
                    Actualités
                </a>
            </div>
            <div class="mt-20 mt-lg-0 col-lg-3 d-flex flex-wrap justify-content-center align-items-center">
                <a href="{{ path('app_logout') }}" class="text-danger">
                    Déconnexion
                </a>
            </div>
        </div>
    </div>
</nav>
